# URL of your backend API server (no trailing slash)
# All /api/* requests from the frontend are proxied here at dev time.
# In production (Docker), nginx forwards /api/* directly to this host.
VITE_API_BASE_URL=http://localhost:3001
